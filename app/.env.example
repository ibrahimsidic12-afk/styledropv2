# SECURITY (Agent #14): no Gemini API key on the client.
# Local debug builds only — the token printed in logcat by DebugAppCheckProviderFactory.
FIREBASE_APPCHECK_DEBUG_TOKEN="YOUR_DEBUG_TOKEN_HERE"
